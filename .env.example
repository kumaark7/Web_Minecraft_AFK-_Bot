# Backend Settings
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Local PostgreSQL settings
POSTGRES_USER=larry
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_DB=larrycontrol
DATABASE_URL=postgresql://larry:change-me-local-only@localhost:5432/larrycontrol

# Future application secrets
# ENCRYPTION_KEY=replace-with-a-local-secret
# JWT_SECRET=replace-with-a-local-secret
